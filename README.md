# Rishi Gupta — Portfolio

A responsive, accessible personal portfolio built with React, TypeScript, and Vite. The site exports to `dist/` and supports GitHub Pages repository subpaths automatically during Actions builds.

## Local development

```bash
pnpm install
pnpm dev
```

## Production checks

```bash
pnpm lint
pnpm build
```
